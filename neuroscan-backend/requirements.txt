# Web framework
fastapi
uvicorn[standard]
python-multipart

# ONNX inference (fast, no PyTorch needed for /predict)
onnxruntime

# PyTorch + timm (needed for /predict/dual — GradCAM visualisation)
# Install CPU-only version to keep image small:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
timm

# Image processing
Pillow
numpy
opencv-python-headless

# Utils
python-dotenv
pydantic
requests
